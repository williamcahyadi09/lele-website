## 1. HeroSection

- [x] 1.1 Add `text-center md:text-left` to the text content wrapper div (badge, headline, subtitle)
- [x] 1.2 Add `justify-center md:justify-start` to the CTA button group flex container

## 2. DreamTeamSection

- [x] 2.1 Add `text-center` to the team member name element inside each card
- [x] 2.2 Add `text-center` to the team member title element inside each card

## 3. ConsultationCTASection

- [x] 3.1 Add `text-center md:text-left` to the section headline
- [x] 3.2 Add `text-center md:text-left` to the description paragraph below the headline

## 4. Verification

- [ ] 4.1 On mobile viewport: confirm HeroSection badge, headline, subtitle, and buttons are centered
- [ ] 4.2 On mobile viewport: confirm DreamTeamSection card names and titles are centered
- [ ] 4.3 On mobile viewport: confirm ConsultationCTASection headline and description are centered
- [ ] 4.4 On desktop viewport: confirm all three sections remain left-aligned (no regression)
- [ ] 4.5 On mobile viewport: confirm ClassesSection card content (features list, card header text) remains left-aligned
